---
layout: default
title: Page not found
permalink: /404.html
---

<h2>The page you’re looking for doesn’t exist at this address.</h2> 

<p>It may have moved, the URL may be misspelled, or the link may be out of date.</p>

<p class="text-small color-fg-muted">
  Requested path: <span id="requested-path"></span>
</p>

<p>You may want to start browsing from <a href="./index">Index page</a>.</p>

{% if site.github and site.github.repository_url %}
<p>
If you think this is a broken link, please <a href="open an issue">{{ site.github.repository_url }}/issues</a> and include the URL you tried to open.
</p>
{% endif %}

<script>
  (function () {
    var base = "{{ site.baseurl | default: '' }}";
    var path = window.location.pathname || "/";

    if (base && base !== "/" && path.indexOf(base + "/") === 0) {
      path = path.slice(base.length);
    } else if (base && path === base) {
      path = "/";
    }

    if (!path) {
      path = "/";
    }

    var pathTarget = document.getElementById("requested-path");
    if (pathTarget) {
      pathTarget.textContent = path;
    }
  })();
</script>
