# Site-wide defaults for jekyll-seo-tag. Each page can override with YAML front matter:
#   title: Page-specific title (used in <title> and og:title)
#   description: Short page-specific description (used in meta description and og:description)
#   image: Page-specific OG/twitter image (default below)
title: BusDK Docs
description: Documentation for BusDK, a modular CLI-first toolkit for running a business in a Git repository with reviewable workspace datasets and auditable workflows.
image: /assets/preview/preview.webp
theme: jekyll-theme-primer
repository: busdk/docs
url: https://docs.busdk.com

# Force our default layout (with styles and structure) for all pages when building with -s docs.
defaults:
  - scope:
      path: ""
    values:
      layout: default
      image: /assets/preview/preview.webp

# Chrome DevTools automatic workspace: the plugin writes .well-known/appspecific/com.chrome.devtools.json
# in the build output only when JEKYLL_ENV=development; production build never contains it.

# Sass: silence deprecation warnings from jekyll-theme-primer (we don't control the gem).
# sourcemap: development — emit .css.map only when JEKYLL_ENV=development so Chrome DevTools
# can map compiled CSS back to .scss sources locally; production builds do not publish source maps.
sass:
  quiet_deps: true
  sourcemap: development